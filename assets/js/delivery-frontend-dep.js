/* Small stub script to act as a dependency placeholder for delivery frontend.
 * Historically this script only ensures ordering after delivery CSS.
 * Keeping it intentionally minimal to avoid server returning HTML (which
 * causes "Unexpected token '<'" errors when the file is missing).
 */
(function(){
    // no-op placeholder
})();
