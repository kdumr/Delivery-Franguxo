<?php
/**
 * Optional performance tweaks for MyD Delivery Pro plugin.
 * This file is intentionally left minimal to avoid debug warnings when absent.
 */

// Example: disable some heavy plugin features if needed (no-op by default)
if ( ! defined( 'ABSPATH' ) ) {
    exit;
}
